package jrm;

public class HttpConnection extends Connection{
    protected HttpConnection(String connectionType, String password, String user) {
        super(connectionType, password, user);
    }
}
