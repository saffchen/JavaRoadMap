package jrm;

public class HttpConnection extends Connection {

    protected HttpConnection(String connectionType, String user, String password) {
        super(connectionType, user, password);
    }
}
