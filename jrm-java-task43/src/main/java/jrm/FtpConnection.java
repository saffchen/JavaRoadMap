package jrm;

public class FtpConnection extends Connection {
    protected FtpConnection(String connectionType, String user, String password) {
        super(connectionType, user, password);
    }
}
